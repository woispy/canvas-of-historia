# Work log — 2026-09-18: ollama dahil edildi

## Goal

NVIDIA kotasından bağımsız, kotasız bir yerel yardımcı ekle.

## Findings

- Ollama CLI 0.34.2 kurulu, ancak servis çalışmıyordu.
- Diskteki modeller: `llama3.1:8b` (kullanılabilir), `qwen2.5-coder:1.5b-base`
  (base model, ajan olarak uygun değil), `nomic-embed-text` (embedding, ajan olamaz).

## Changes

- `opencode.json`: `ollama` provider eklendi (OpenAI-compatible,
  `http://localhost:11434/v1`, dummy apiKey). Model: `ollama/llama3.1:8b`.
- `.opencode/agents/local-assist.md`: salt-okunur + work-log yazabilen yerel
  yardımcı. Rolü: özet, durum raporu, basit keşif, kota dolduğunda offline yedek.
  Zor işte tahmin etmek yerine doğru takım arkadaşına devreder.
- `AGENTS.md` ekip listesi güncellendi.

## Test results

- `opencode agent list` → `local-assist (subagent)` tanındı (modele bağlanmadan
  yapılan config doğrulaması; gerçek çıkarım için `ollama serve` gerekli).

## Verification (same day)

- User started `ollama serve`. `ollama list` → 3 models visible.
- `GET /v1/models` (the exact endpoint OpenCode uses) → `llama3.1:8b` listed.
- Smoke inference `llama3.1:8b` → returned `LOCAL-OK`. End-to-end path works.
- Remaining step: restart the OpenCode session so `@local-assist` loads.

- Kullanıcı `ollama serve` ile servisi başlatacak (ayrı terminal veya Ollama uygulaması).
- İleride istenirse daha güçlü yerel coder denenebilir
  (`ollama pull qwen2.5-coder:7b-instruct`), donanıma göre karar verilir.
